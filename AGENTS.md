# 项目规矩：山野茶铺官网

## 这是什么
一家社区茶铺的官网（纯静态 HTML/CSS/少量 JS），给顾客看菜单、找门店、预约包场；不接后台，用 GitHub Pages 发布。

## 配色与排版
- 主色 `#4f6f52`（茶汤绿），强调色 `#d9a441`（蜜金），背景 `#f7f4ec`（宣纸白），正文 `#2b2b2b`
- 字体：系统中文字体优先（`"PingFang SC", "Microsoft YaHei", sans-serif`），不引外部字体
- 所有页面共用 `style.css`，不写行内样式

## 文案禁区
- 不写「全城最好」「第一」「顶级」这类绝对化用词，不写疗效
- 价格写「元起」或具体数字，不写「优惠」百分比；营业时间以门店信息页为唯一来源
- 不出现真实手机号、身份证号；预约只做表单展示位与邮箱占位
- 中文标点用全角，中英文之间留一个空格

## 结构
- `index.html` 首页　`menu.html` 菜单价目　`shop.html` 门店信息　`book.html` 预约包场
- 图片放 `assets/`，二维码由 `tools/make_qr.py` 生成，不手工上传
- 每页底部统一页脚，含「返回顶部」
